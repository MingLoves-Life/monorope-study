{
  "name": "lerna",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx lerna run dev --parallel"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "private": "true",
  "workspaces": [
    "packages/*"
  ]
}
